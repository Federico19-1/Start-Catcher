4520:            // panel (positioned at panelY + 425 sub-header, panelY + 440
4521:            // toggle). With SETTINGS_CONTENT_H = 510 the content is
4522:            // taller than panelH = 449, so we CLIP the panel rect AND
4523:            // translate up by scrollY before drawing the rows that
4524:            // follow. The per-account note is drawn BEFORE the clip so
4525:            // it never scrolls off-screen at the top.
4526:            ctx.save();
4527:            // Per-account note (always visible at the very top of the
4528:            // panel) — see comment above on why it sits inside now.
4529:            ctx.font = '11px Orbitron, sans-serif';
4530:            ctx.textAlign = 'left';
4531:            ctx.fillStyle = currentUser
4532:                ? 'rgba(73, 215, 200, 0.85)'
4533:                : 'rgba(255, 255, 255, 0.5)';
4534:            ctx.fillText(
4535:                currentUser
4536:                    ? '✓ Saved for ' + currentUser
4537:                    : 'Settings saved locally (no account)',
4538:                panelX + 18, panelY + 14
4539:            );
4540:            ctx.restore();
4541:
4542:            ctx.save();
4543:            // Clip to the panel INNER rect so a row that overflows the
4544:            // bottom is hidden (instead of leaking over the BACK button).
4545:            // We use a plain rect — roundRect would clip along the curve
4546:            // which our quad rows can't follow anyway.
4547:            ctx.beginPath();
4548:            ctx.rect(panelX, panelY, panelW, panelH);
4549:            ctx.clip();
4550:            // Scroll content up by settingsScrollY.  All row draws below
4551:            // share the same translate, but keyboard + mouse hit-tests
4552:            // (via menuButtons.push) use un-translated coords (which is
4553:            // what we want — the toggle hit-rect matches the visible row
4554:            // after the translate).
4555:            ctx.translate(0, -settingsScrollY);
4556:
4557:            const padX = 30;
4558:            const innerW = panelW - padX * 2;
4559:            const rowX = panelX + padX;
4560:
4561:            drawSettingsRow(rowX, panelY + 15, innerW, 56,
4562:                '🎵 MUSIC',
4563:                'Background song in the main menu',
4564:                settings.music, 'toggleMusic');
4565:
4566:            drawVolumeRow(rowX, panelY + 76, innerW, 36,
4567:                settings.menuVolume, 'menuVolDown', 'menuVolUp');
4568:
4569:            drawSettingsRow(rowX, panelY + 127, innerW, 56,
4570:                '🔊 SFX',
4571:                'All other sounds during gameplay',
4572:                settings.sfx, 'toggleSfx');
4573:
4574:            drawSettingsRow(rowX, panelY + 198, innerW, 56,
4575:                '⭐ CLASSIC MUSIC',
4576:                'Plays during Classic Mode runs',
4577:                settings.classicMusic, 'toggleClassicMusic');
4578:
4579:            drawVolumeRow(rowX, panelY + 259, innerW, 36,
4580:                settings.classicVolume, 'classicVolDown', 'classicVolUp');
4581:
4582:            drawSettingsRow(rowX, panelY + 310, innerW, 56,
4583:                '💀 SURVIVAL MUSIC',
4584:                'Plays during Survival Mode runs',
4585:                settings.survivalMusic, 'toggleSurvivalMusic');
4586:
4587:            // ----- 🎮 GAME CONTROLS section (closes with ctx.restore below) -
4588:            // Visually separate from the SOUNDS controls above so the new
4589:            // gameplay-affecting toggle is clearly its own category.
4590:            ctx.save();
4591:            ctx.fillStyle = 'rgba(73, 215, 200, 0.9)';
4592:            ctx.shadowColor = 'rgba(73, 215, 200, 0.6)';
4593:            ctx.shadowBlur = 6;
4594:            ctx.font = 'bold 14px Orbitron, sans-serif';
4595:            ctx.textAlign = 'left';
4596:            ctx.fillText('🎮 GAME CONTROLS', rowX, panelY + 425);
4597:            ctx.restore();
4598:
4599:            drawSettingsRow(rowX, panelY + 440, innerW, 56,
4600:                '🖱 FREE MOVEMENT (Survival)',
4601:                'Ship follows the cursor exactly — no smoothing, full screen.',
4602:                settings.survivalFreeMovement, 'toggleSurvivalFreeMovement');
4603:
4604:            ctx.restore();  // End of scroll-clip + translate group
4605:            ctx.restore();  // End of outer panelCtx.save() wrapper
4606:
4607:            // ----- 🎮 GAME CONTROLS section ----------------------------------
4608:            // Visual separator between audio controls and gameplay-affecting
4609:            // controls. Same amber/teal accent used in the Settings panel
4610:            // border so the new section reads as a natural continuation
4611:            // instead of a foreign block. ctx.save()/ctx.restore() scopes
4612:            // the small 'fillStyle' / 'shadowBlur' we briefly change so
4613:            // the existing SOUNDS rows above (and the per-account note
4614:            // below) keep their original colours.
4615:            ctx.save();
4616:            ctx.fillStyle = 'rgba(73, 215, 200, 0.9)';   // teal accent – pairs
4617:            ctx.shadowColor = 'rgba(73, 215, 200, 0.6)';  // with the panel glow
4618:            ctx.shadowBlur = 6;
4619:            ctx.font = 'bold 14px Orbitron, sans-serif';
4620:            ctx.textAlign = 'left';
4621:            ctx.fillText('🎮 GAME CONTROLS', rowX, panelY + 425);
4622:            ctx.restore();
4623:
4624:            // Free-movement toggle. Same drawSettingsRow helper as the
4625:            // SOUNDS rows so the pill geometry / ON-OFF colours are
4626:            // perfectly consistent — only the title/description differ
4627:            // to make it obvious that this affects MOVEMENT not audio.
4628:            drawSettingsRow(rowX, panelY + 440, innerW, 56,
4629:                '🖱 FREE MOVEMENT (Survival)',
4630:                'Ship follows the cursor exactly — no smoothing, full screen.',
4631:                settings.survivalFreeMovement, 'toggleSurvivalFreeMovement');
4632:
4633:            // Per-account note — moved INSIDE the panel (panelY + 12)
4634:            // after the scroll rework. The original formula
4635:            // (panelY + panelH + 13 = 562) put the text under the BACK
4636:            // button at y=550+, making the "Saved for ..." line silent;
4637:            // drawing it at the top edge of the panel keeps it visible
4638:            // REGARDLESS of how far the user has scrolled because the
4639:            // TRANSLATE is applied below AFTER this text is drawn.
4640:            ctx.font = '12px Orbitron, sans-serif';
